import { NoTagDirective } from './no-tag.directive';

describe('NoTagDirective', () => {
  it('should create an instance', () => {
    const directive = new NoTagDirective();
    expect(directive).toBeTruthy();
  });
});
